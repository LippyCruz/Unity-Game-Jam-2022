namespace ActionManagement
{
    /// <summary>
    /// Defines the different types of actions that the player can do
    /// </summary>
    public enum ActionType
    {
        DRAW,
        SUPPORT,
        CULTIVATE,
        BUILD,
        REST
    }
}